# @huanlin/dsh-plugin-sidebar-brand-text · dsh 0.1.2-alpha.1 → alpha.2 迁移指南

> 对照基准：dsh tag `dsh-v0.1.2-alpha.1`（cd5ef81481）→ `dsh-v0.1.2-alpha.2`（0a53fb55be），234 commits。
> 插件版本：`@huanlin/dsh-plugin-sidebar-brand-text@0.5.0`。生成日期：2026-08-30。
> 所有 alpha2 证据均来自对 `D:/Projects/deepseek-harness/dsh` 的只读 git 命令（`git show` / `git diff` / `git grep`），未对 dsh 树做任何写操作。

## 结论

**⚠️ 需适配（仅 1 处，约 3 行）。** alpha2 从 `@deepseek-ai/dsh-settings` 包根移除了 `settingsNamespace()` 工厂函数（上游改为 `register()` 泛型参数直接做模板字面量校验），插件 `src/settings.ts` 的 import 与常量声明必须改写为字符串字面量——这是本插件在此次升级中**唯一**的代码破坏点。其余全部依赖面（`sidebar.brand.name` 槽位、`settings.plugin.item` keyed 槽位、`webServer.register({kind:'prefix'})`、client-store、locale API、`sessions.list` 快照、ui-slots 全部注入类型、react 18、cordis 4.x）在 alpha2 中契约不变；改完重建 `lib/` 即完成升级。

## 插件现状

**版本与形态**：`0.5.0`，单包双半侧（host `lib/index.js` + client `lib/client.js`），预构建 `lib/` 入库策略，`files: ["lib/", "cordis.patch.yml"]`，`dsh.bundle.patch: ./cordis.patch.yml`。

**peerDependencies**（12 项，全部 `peerDependenciesMeta.optional: true`）：

| 包 | 范围 | alpha2 实际 | 匹配 |
|---|---|---|---|
| `@deepseek-ai/cordis` | `^4.0.1` | vendor 4.0.2 | ✅ |
| `@deepseek-ai/schemastery` | `^3.18.1` | vendor 3.18.2 | ✅ |
| `react` | `^18.2.0` | 仍 18.x（`apps/web/package.json:51` `"react": "^18.2.0"`） | ✅ |
| `@deepseek-ai/dsh-api-session-controller` | `^0.1.2-alpha.1` | 0.1.2-alpha.2 | ✅ |
| `@deepseek-ai/dsh-client-locale` | `^0.1.2-alpha.1` | 0.1.2-alpha.2 | ✅ |
| `@deepseek-ai/dsh-client-store` | `^0.1.2-alpha.1` | 0.1.2-alpha.2 | ✅ |
| `@deepseek-ai/dsh-client-ui-renderer` | `^0.1.2-alpha.1` | 0.1.2-alpha.2 | ✅ |
| `@deepseek-ai/dsh-client-ui-settings-plugins` | `^0.1.2-alpha.1` | 0.1.2-alpha.2 | ✅ |
| `@deepseek-ai/dsh-client-ui-sidebar` | `^0.1.2-alpha.1` | 0.1.2-alpha.2 | ✅ |
| `@deepseek-ai/dsh-client-ui-slots` | `^0.1.2-alpha.1` | 0.1.2-alpha.2 | ✅ |
| `@deepseek-ai/dsh-invariants` | `^0.1.2-alpha.1` | 0.1.2-alpha.2 | ✅ |
| `@deepseek-ai/dsh-settings` | `^0.1.2-alpha.1` | 0.1.2-alpha.2 | ✅ |

> semver 说明：`^0.1.2-alpha.1` 依 npm prerelease 范围规则（同 `[major,minor,patch]` 元组且范围显式含 prerelease）本就容纳 `0.1.2-alpha.2`，**不改也不会破**；建议 bump 到 `^0.1.2-alpha.2` 仅为了表意清晰。

**依赖引用方式**：`package.json` 的 dependencies/devDependencies/overrides 中**没有**任何 `link:...source/current` 引用（devDependencies 只有 registry 的 cordis/schemastery 等）；`@deepseek-ai/dsh-*` 只作 peer 声明不安装。类型解析走 `tsconfig.json` `paths` → `C:/Users/Administrator/.dsh/source/current/...`（11 条映射）。profile 端以 `link:` 挂载本插件（README「运行」节）。

**用到的 dsh API 清单**（逐文件）：

| 层 | 文件:行 | API |
|---|---|---|
| host 入口 | `src/index.ts:26` | `inject: ['webServer']`；cordis composition（`name`/`Config`/`apply` 导出约定） |
| host settings | `src/settings.ts:14,19` | `import { settingsNamespace, type SettingsScope } from '@deepseek-ai/dsh-settings'`；`SETTINGS_NAMESPACE = settingsNamespace('sidebar-brand-text')` |
| host settings | `src/settings.ts:56,62,69` | `sctx.settings.register(ns, Config, { base })`、`scope.get()`、`scope.watch()`；catch `"already registered"` 去重 |
| host gateway | `src/gateway.ts:69-72,77,145` | `ctx.inject(['settings'])`；`ctx.webServer.register({ kind:'prefix', path:'/sbbt/api', handler })`；`settings.update(ns, patch)` |
| host schema | `src/config.ts:11` | `@deepseek-ai/schemastery` `z.object/z.string().default/.description` |
| host invariant | `src/invariant.ts:24` | `ctx.invariants.register(PACKAGE_NAME, install)` |
| client 注入 | `src/client/index.ts:50` | `inject: ['slots', 'locale', 'sessions']`；`ctx.get('betterLocale')`（可选第三方，有 undefined 守卫） |
| client 槽位 | `src/client/index.ts:89,100` | `ctx.slots.inject('sidebar.brand.name', …)`（single）；`ctx.slots.inject('settings.plugin.item', function*(){…})` + `register({ name, key:'sidebar-brand-text', locale: NS, inject })`（keyed） |
| client 槽类型 | `BrandText.tsx:16-18`、`BrandTextCard.tsx:16-17`、`bindSnapshotSelector.ts:11` | ui-slots 的 `PropsRuntime` / `PropsLocale` / `SnapshotSelectorHook` / `HostObservable`；`LocaleNamespaceMap` 增补（`src/client/index.ts:42-47`） |
| client store | `src/client/controller.ts:15` | `createSnapshotStore` / `SnapshotStore`（@deepseek-ai/dsh-client-store） |
| client locale | `src/client/index.ts:61,73` | `ctx.locale.register(NS, { zh, en })`、`ctx.locale.subscribe` |
| client sessions | `src/client/titleWriter.ts:36-41,66-68` | `sessions.list.getSnapshot()/subscribe()`；快照面 `{ current, byId: Record<id, { title? }> }` |
| type-only | `src/client/index.ts:22-27` | `/client` 子路径类型面：`dsh-api-session-controller/client`、`dsh-client-locale/client`、`dsh-client-ui-renderer/client`、`dsh-client-ui-sidebar/client`、`dsh-client-ui-settings-plugins/client` |
| 隐性行为依赖 | `src/client/titleWriter.ts` 全文 | ui-renderer `DocumentTitle.tsx` 的构建期 `DSH_CLIENT_TITLE` 覆写时序（queueMicrotask + MutationObserver 竞争兜底） |
| 样式 | `BrandTextCard.tsx` 等 | `var(--dsw-alias-*, fallback)` 设计令牌（带 fallback，纯装饰层） |
| 槽位契约 | `cordis.patch.yml` | 仅 `- insert` 行（id/name/config），loader 从 profile node_modules 解析 `name` |

## 影响对照表

| alpha1→alpha2 变更 | 是否影响本插件 | 证据（文件:行 或 只读 git 命令输出摘录） |
|---|---|---|
| **`settingsNamespace()` 从 `@deepseek-ai/dsh-settings` 根导出中移除**；`register()` 改为 `register<const Namespace extends string, T>(ns: Namespace & SettingsNamespaceInput<Namespace>, …)` 泛型 + 模板字面量类型校验，运行时 `parseSettingsNamespace` 校验 | ❌ **影响（唯一代码破坏点）**：`src/settings.ts:14` import 与 `:19` 调用将编译/运行失败 | `git grep -n "settingsNamespace" dsh-v0.1.2-alpha.2 -- packages/settings/settings/src` → **(no match)**；alpha1 `packages/settings/settings/src/index.ts:26` 有 `export function settingsNamespace`；alpha2 diff 显示该函数被模板字面量类型 `SettingsNamespaceInput` + 私有 `parseSettingsNamespace` 取代 |
| settings 服务其余 API：`SettingsScope.get/watch/update`、`SettingsProvider.register/update(ns, patch)`、重复注册报错文案 | ✅ 兼容，无需改 | alpha2 `index.ts:115-133` interface 含 `get()/watch()/update(patch)`；`async update<const Namespace extends string>(ns: Namespace & SettingsNamespaceInput<Namespace>, …)`；重复注册仍 `throw new Error(\`settings namespace "${parsedNs}" is already registered\`)`（插件 `settings.ts:58` 的 `includes('already registered')` 判定仍命中）；上游 locale 包 alpha2 同样直接 `settingsCtx.settings.register(LOCALE_SETTINGS_NAMESPACE, …)`（`packages/client/locale/src/index.ts:18`） |
| ui-tool / ui-workspace 注入 hook `connectionGeneration` → `hostInfo`，`ToolConnectionGenerationInjected` 更名 `ToolHostInfoInjected` | ✅ 不影响 | 插件全部源码（rg 复核）无 `connectionGeneration`/`ToolConnectionGeneration` 字样；插件槽位注入用的是自有 inject face（`BrandTextInjected`/`BrandTextCardInjected`），不经 ui-tool/ui-workspace 的 hook |
| `settings.plugin.item` keyed 槽位所在包 ui-settings-plugins 大改（11 文件 +932/−184） | ✅ 兼容 | alpha2 `packages/client/ui-settings-plugins/src/client/index.ts:164` 仍声明 `children: { 'settings.plugin.item': { kind: 'keyed', scope: 'root' } }`；:167-190 上游自身注册形态与插件逐字段一致（`{ name, key, locale, inject }`）；`./client` export 存续（alpha2 package.json） |
| `sidebar.brand.name` 槽位（ui-sidebar 仅 package.json 变更） | ✅ 兼容 | alpha2 `packages/client/ui-sidebar/src/client/contract/slots.ts:28` `'sidebar.brand.name': { kind: 'single'; scope: 'root' … }`；`SidebarRoot.tsx:155` 仍 `renderSlot('sidebar.brand.name', …)` |
| client/connection 重构（18 文件 +838/−162）+ Web 连接恢复指示器 | ✅ 不影响 | 插件未 import `@deepseek-ai/dsh-client-connection`（rg 复核无匹配） |
| session 内部 folds→projections 迁移（api/session-controller 45 文件） | ✅ 客户端 `sessions.list` 面未变 | alpha2 `packages/api/session-controller/src/client/sessions/service.ts:73` `byId: Record<SessionId, SessionSummary>`、:235 `current`/`byId` 初始化、:434 `…byId[…]?.title`；插件仅 type-only import `/client` 面且 duck-type 最小面 |
| session 事件 envelope 增加 `ignorable: true` | ✅ 不影响 | 插件不消费任何 session 事件（只订阅 `sessions.list` 快照与自有 store） |
| gateway/Remote 失败词汇收敛、failure marker 改名、`$host` 身份稳定 | ✅ 不影响 | 插件网关是自托管 HTTP 路由（`gateway.ts:77` `webServer.register({kind:'prefix', path:'/sbbt/api'})`），不经 apiproxy Remote；`packages/host/webserver` 两 tag 间 diff 仅 `package.json` 1 行；alpha2 `packages/client/modules/src/index.ts:586` 仍是同款 `kind: 'prefix'` 调用 |
| ui-slots / ui-renderer / ui-sidebar / store 仅清单级变更 | ✅ 兼容 | `git diff --stat dsh-v0.1.2-alpha.1 dsh-v0.1.2-alpha.2 -- <四包>` 仅 package.json/README 行；`DocumentTitle.tsx` diff 为空（titleWriter 竞争逻辑前提不变）；alpha2 `store/src/index.ts:27,103` `SnapshotStore/createSnapshotStore` 签名未变；ui-slots 仍导出 `PropsLocale`(index.ts:82)/`PropsRuntime`(:214)/`SnapshotSelectorHook`(:18)/`HostObservable`(:20) |
| locale 包 9 文件（主要 i18n 文案） | ✅ 兼容 | alpha2 `packages/client/locale/src/client/index.ts:380-381` `register(ns, localeOrDicts, dict?)` 双签名、:560 `locale.subscribe(sync)` 均存续 |
| client 包 peerDependencies 收敛（如 ui-chat ~25→1，只剩 cordis） | ✅ 不影响 | 收敛发生在 dsh client 包**之间**；本插件是 peer 的声明方与消费方，`dsh.client.inject` 的 7 个包在 alpha2 全部存在且未改名（摘要「包结构零破坏」+ 上述各包 package.json 核验） |
| vendor：cordis 4.0.2 / schemastery 3.18.2 / 新增 util-values 等 4 包 | ✅ 兼容 | `git show dsh-v0.1.2-alpha.2:vendor/cordis/package.json` → 4.0.2；vendor/schemastery → 3.18.2；仍在 4.x/3.18.x 内；插件不 import 新增包；`settings` 包 alpha2 index.ts 新增 `import { deepEqualJson, deepFreeze } from '@deepseek-ai/dsh-util-values'` 属其内部实现 |
| Windows 修复（atomic-write 重试、stat 父目录探测）、npm dist-tag 发布路由 | ✅ 不影响 | 插件不使用 atomic-write/stat probe；发布路由只影响 dsh 自身发包 |
| ui-primitives 19 文件改动（设计令牌） | ✅ 不影响（装饰层） | alpha2 `packages/client/ui-primitives/src/{Button,Input,Menu}.module.css` 仍含 `dsw-alias-border-l2`；插件全部 `var()` 带 fallback |

## 迁移步骤

> 前提（一次性，非本插件改动）：`~/.dsh/source/current`（与 dev clone）已按 SOP 升到 `dsh-v0.1.2-alpha.2`（0a53fb55be）并完成 `pnpm install` + `pnpm run build`——本插件 typecheck 的 `tsconfig.json` `paths` 全部指向该树，升级后类型自动切到 alpha2，`tsconfig.json` 本身**无需改动**。

1. **改 `src/settings.ts`（唯一代码改动，3 行）**：
   - 第 14 行：
     ```ts
     // 改前
     import { settingsNamespace, type SettingsScope } from '@deepseek-ai/dsh-settings'
     // 改后
     import type { SettingsScope } from '@deepseek-ai/dsh-settings'
     ```
   - 第 19 行：
     ```ts
     // 改前
     export const SETTINGS_NAMESPACE = settingsNamespace('sidebar-brand-text')
     // 改后（alpha2 上游同款写法，参照 packages/client/locale/src/locale-settings.ts:7 的 LOCALE_SETTINGS_NAMESPACE = 'locale'）
     export const SETTINGS_NAMESPACE = 'sidebar-brand-text' as const
     ```
   - 文件其余部分（`register(ns, Config, { base })`、`scope.get/watch`、`settings.update(SETTINGS_NAMESPACE, patch)`、`"already registered"` 去重 catch、`isUnloading` 数值守卫）**无需改动**：alpha2 的 `register`/`update` 泛型直接收 kebab 字符串字面量（`'sidebar-brand-text'` 通过模板字面量校验），报错文案逐字兼容。
   - `src/gateway.ts`、`src/config.ts`、`src/index.ts`、`src/invariant.ts`、`src/client/**`、`cordis.patch.yml`、`tsconfig.json`、`package.json` 的 inject/files/scripts：**均无需改动**（依据见上表）。`SETTINGS_NAMESPACE` 的字符串值不变，用户 `$DSH_HOME/settings.yaml` 里已持久化的 `sidebar-brand-text` namespace 数据跨升级保留，无需迁移。
2. **`package.json` 元数据（可选但建议）**：`version` `0.5.0` → `0.5.1`；8 个 `@deepseek-ai/dsh-*` peer 由 `^0.1.2-alpha.1` 对齐为 `^0.1.2-alpha.2`（不改也满足 semver，见上表）；`@deepseek-ai/cordis ^4.0.1`、`@deepseek-ai/schemastery ^3.18.1`、`react ^18.2.0` 不动。
3. **README.md**：「开发」节「本插件适配 DSH `v0.1.2-alpha.1`」改为 `v0.1.2-alpha.2`。
4. **重建 `lib/`**（`workdir=D:\Projects\deepseek-harness\dsh-plugin-sidebar-brand-text`）：
   ```powershell
   pnpm run typecheck   # tsc --noEmit，类型经 tsconfig paths 解析到 alpha2 源码树
   pnpm test            # vitest（dsh-client-store 用本地测试替身，不受影响）
   pnpm run build       # tsdown + tsc → lib/index.js（含 settings.ts 改动）、lib/client.js、lib/invariant.js、lib/types/
   ```
   确认 `lib/client.js` 仍以 `window.__ModuleLoader__.load({ id: … })` 包裹。若 typecheck 报 `@deepseek-ai/dsh-util-values`/`@deepseek-ai/dsh-brand` 解析失败（alpha2 settings 的 .d.ts 新增传递引用；`skipLibCheck: true` 通常已兜底）——**待人工确认**：出现时在 `tsconfig.json` `paths` 补这两条映射即可，其余不动。
5. **profile 端生效**：profile 以 `link:D:\Projects\deepseek-harness\dsh-plugin-sidebar-brand-text` 引用，重建 `lib/` 后 profile `node_modules/@huanlin/dsh-plugin-sidebar-brand-text/lib/` 即时可见，**无需 `dsh plugin add/update` 重装**。之后由人类重启 `dsh web` 进程 + 浏览器硬刷新（`Ctrl+Shift+R`）。
6. **对外发布**（huanlinoto SOP）：`pnpm run build` → `git add lib/ package.json README.md` → commit（如 `v0.5.1: adapt to dsh 0.1.2-alpha.2 (settingsNamespace removed)`）→ `git push origin master`；npm 侧 `npm pack --dry-run` 核对 `files`（`lib/` + `cordis.patch.yml`）后 `pnpm publish`。`dsh/` 树全程零写操作。

## 升级后验证清单

> 前置：dsh checkout 已升级到 alpha.2 并完成 install/build；`dsh web` 进程**已重启**、浏览器已 **Ctrl+Shift+R** 硬刷新（由人类执行）。

1. **加载无报错**：控制台无 `Failed to load plugins` / `missed the module table` / `Cannot find package '@deepseek-ai/dsh-*'`（client bundle 仍以 `window.__ModuleLoader__.load` 契约加载）。
2. **侧边栏品牌行**：左上角显示 settings.yaml/卡片配置的 `name`（默认 seed `DSH Local Build`）；`revision` 非空显示徽标、空字符串无徽标；鱼形 mark 保持原样。
3. **配置卡片**：设置 → 插件配置页出现「侧边栏品牌文案」卡片（与内置卡同列），展开后两个输入框可编辑；保存后侧边栏文案**与浏览器标签页标题**实时更新，无需重启；「已保存」状态出现。
4. **标签页标题**：`<会话标题> — <配置品牌名>`；切换会话实时更新；无会话时仅显示品牌名（titleWriter 对 alpha2 `DocumentTitle` 的 queueMicrotask + MutationObserver 竞争仍成立——该文件两版本 diff 为空）。
5. **持久化**：保存后检查 `$DSH_HOME/settings.yaml` 出现/更新 `sidebar-brand-text` namespace；再次重启 `dsh web` 后配置仍在（跨升级存量 seed 保留）。
6. **折叠态**：侧边栏折叠到 56px 轨道只剩 mark（本插件不可见），展开后恢复。
7. **HTTP 通道**：DevTools 控制台执行 `fetch('/sbbt/api/get',{method:'POST',headers:{'content-type':'application/json'},body:'{}'}).then(r=>r.json())` 返回 `{ok:true,value:{config:{name,revision}}}`；`/sbbt/api/set` 保存后 settings.yaml 同步。
8. **回退**：从 profile 移除插件并重启后，侧边栏恢复 `DSH Local Build` + 7 位 commit hash、标签页恢复构建期标题。
9. **工程检查**：`pnpm run typecheck && pnpm test && pnpm run build` 全绿；`git -C D:/Projects/deepseek-harness/dsh status` 干净（零源码 patch）。

### 待人工确认（低风险）

- `src/settings.ts:26-29` 的 `isUnloading` 数值守卫（fiber state 4/5）依赖 cordis 内部枚举数值——cordis 4.0.1→4.0.2 为 patch 升级，预期不变，未逐一核对枚举源码。
- alpha2 settings 包 .d.ts 新增对 `@deepseek-ai/dsh-util-values`/`dsh-brand` 的传递类型引用，`tsconfig.json` 未映射这些包；`skipLibCheck: true` 预期兜底，若 typecheck 报解析错误按迁移步骤 4 补 paths。
- 可选增强项 `ctx.get('betterLocale')`（第三方 dsh-plugin-better-locale）自身是否已适配 alpha2：本插件已有 undefined 守卫，该服务缺失时仅词典覆盖不生效，主功能不受影响。
