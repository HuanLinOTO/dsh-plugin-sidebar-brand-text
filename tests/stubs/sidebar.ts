/** Test double for @deepseek-ai/dsh-client-ui-sidebar/client (see vitest.config.ts alias).
 * The plugin imports only types from this module (the SlotMap declaration merge);
 * no runtime values are used. */
export {}
